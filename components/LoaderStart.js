import React from "react";
import { StyleSheet, Text, View } from "react-native";

const LoaderStart = () => {
  return (
    <View>
      <Text>Este es el LoaderStart</Text>
    </View>
  );
};

export default LoaderStart;

const styles = StyleSheet.create({});
